# Profiles

Saved column mapping profiles will live here.

Phase 5 currently lets the user confirm mappings in `review.html` and download a
`mapping-profile.json` file from the browser. A later step will add a local save
flow that stores confirmed profiles in this folder automatically.
